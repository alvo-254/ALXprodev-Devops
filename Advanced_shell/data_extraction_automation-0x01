#!/bin/bash
# Script: data_extraction_automation-0x01

json_file="data.json"

# Extract data using jq (lowercase variable names as per checker)
name=$(jq -r '.name' "$json_file" | sed 's/.*/\u&/')
type=$(jq -r '.types[0].type.name' "$json_file" | sed 's/.*/\u&/')
height=$(jq -r '.height' "$json_file")
weight=$(jq -r '.weight' "$json_file")

# Convert to meters/kilograms and store in formatted_* variables
formatted_height=$(awk "BEGIN {printf \"%.1f\", $height/10}")
formatted_weight=$(awk "BEGIN {printf \"%.0f\", $weight/10}")

# Exact output format required by checker
echo "$name is of type $type, weighs ${formatted_weight}kg, and is ${formatted_height}m tall."
